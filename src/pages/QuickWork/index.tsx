import { Card } from 'antd';
import { FC } from 'react';

const QuickWork: FC = () => {
  return (
    <div>
      <Card title="快速工作入口" bordered={false}>
        <p>快速工作入口页面内容</p>
      </Card>
    </div>
  );
};

export default QuickWork;
