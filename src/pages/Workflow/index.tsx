import { Card } from 'antd';
import { FC } from 'react';

const Workflow: FC = () => {
  return (
    <div>
      <Card title="事务流程" variant="borderless">
        <p>事务流程页面内容</p>
      </Card>
    </div>
  );
};

export default Workflow;
