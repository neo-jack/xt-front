@xt-umi/ 帮我实现一个登陆页面访问一个layout控件，但是这个控件被withAuthorization包裹，他实现权限控制和自动登陆，当用户访问/或者/login时候访问/login，当用户访问/xt/dashboard，判断是否有token，有token则自动登陆，没有token则回到/login，其他网页则到404页面，默认访问页面是/login，会自动触发自动登录，你需要遵守xt-umi\.cursor文件夹下的规定，来实现的我的需求，我不要你直接修改代码，而且把你需要修改的步骤写到xt-umi\.步骤.txt，而且不要忽略步骤，保证正确执行步骤后，能实现效果，写步骤的时候，写清楚为什么这样写，我需要根据这些步骤来写代码。