<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dyuloon.mapper.UserMapper">

    <!-- 根据用户名查找用户 -->
    <select id="selectByUsername" resultType="com.dyuloon.entity.User">
        SELECT id, username, password, user_name as userName, user_avatar as userAvatar, 
               user_role as userRole, hospital_id as hospitalId, hospital_cname as hospitalCname,
               created_at as createdAt, updated_at as updatedAt
        FROM users 
        WHERE username = #{username}
    </select>

</mapper>
